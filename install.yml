
- hosts: all
  become: yes
  vars:
    print_debug: true
    upload_cephadm: false
    install_ceph: false
    init_ceph: false
    add_ceph_nodes: true
    add_osd_nodes: false                    # --> Need to check if it works
  roles:
    - { role: init-hosts }
#    - { role: ceph }
#    - { role: block }
#    - { role: nfs }
#    - { role: radosgw }
#    - { role: cephfs }
#    - { role: custom }

