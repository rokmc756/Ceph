---
- hosts: all
  become: yes
  vars:
    print_debug: true
    upload_cephadm: false
    install_ceph: false
    init_ceph: false
    add_ceph_nodes: true
    add_osd_nodes: false                           # --> Need to check if it works
  roles:
    - { role: ceph }
  # - { role: rbd }
  # - { role: radosgw }
  # - { role: nfs }
  # - { role: cephfs }
  # - { role: custom }
  # - { role: init-hosts }
  # - { role: docker }

