---
- hosts: all
  become: yes
  vars:
    print_debug: true
    upload_cephadm: false
    install_ceph: false
    init_ceph: true
    add_ceph_nodes: false
    add_osd_nodes: false                           # --> Need to check if it works
  roles:
    - { role: ceph }

  # - { role: rbd }
  # - { role: custom }
  # - { role: radosgw }
  # - { role: nfs }
  # - { role: init-hosts }
  # - { role: docker }

