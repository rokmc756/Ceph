---
- hosts: all
  become: yes
  vars:
    print_debug: true
    install_cephadm: false
    deploy_cephadm: false
    init_ceph: false
    check_containers: false
    add_monitor_nodes: false
    add_osd_nodes: false
    apply_additional_features: false
    enable_rbd_pool: false
    enable_custom_pool: false
    enable_radosgw: false
    enable_nfs: true
  roles:
    - { role: ceph }
#    - { role: init-hosts }
#    - { role: docker }
