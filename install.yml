
- hosts: all
  become: yes
  vars:
    print_debug: true
    upload_cephadm: false
    install_ceph: false
    init_ceph: false
    add_ceph_nodes: false
    add_osd_nodes: false                    # --> Need to check if it works
  roles:
    - { role: nfs }
  #  - { role: cephfs }
  #  - { role: rbd }
  #  - { role: ceph }
  # - { role: init-hosts }
  # - { role: radosgw }
  # - { role: custom }
  # - { role: docker }

