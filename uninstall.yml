- hosts: all
  become: yes
  vars:
    print_debug: true
    remove_osd_nodes: false
    # remove_mon_nodes: false
    # remove_mgr_nodes: false
    remove_ceph_nodes: true
    purge_ceph: false
    uninstall_ceph: false
    remove_cephadm_bin: false
  roles:
    - { role: ceph }

#  - { role: rbd }
#  - { role: custom }
#  - { role: radosgw }
#  - { role: nfs }
#  - { role: init-hosts }
#  - { role: docker }

