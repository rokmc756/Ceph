- hosts: all
  become: yes
  vars:
    print_debug: true
    remove_rgw: true
    remove_nfs_cluster: false
    remove_custom_pool: false
    remove_block_device_image: false
    destroy_ceph_cluster: false
  roles:
    - { role: ceph }

#    - { role: init-hosts }
#    - { role: docker }
